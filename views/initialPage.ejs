<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>IP List</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                margin: 0;
                padding: 0;
                height: 100vh;
                display: flex;
                justify-content: center;
                align-items: center;
                background-color: #f4f4f4;
            }

            .container {
                display: flex;
                /* width: 55%; */
                height: 90vh;
                max-width: 1200px;
                background-color: white;
                border-radius: 8px;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }

            .form-container {
                width: 350px;
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                padding: 20px;
                border-right: 2px solid #ddd;
            }

            .ips-container {
                display: flex;
                flex-direction: column;
                width: 350px;
                padding: 20px;
                overflow-y: auto;
                max-height: 80vh;
            }

            h1 {
                color: #333;
                text-align: center;
                margin-bottom: 20px;
            }

            .input-field {
                margin-bottom: 15px;
                width: 100%;
                max-width: 300px;
                padding: 10px;
                font-size: 16px;
                border-radius: 5px;
                border: 1px solid #ccc;
            }

            .select-file {
                margin-bottom: 15px;
                width: 100%;
                max-width: 300px;
            }

            .submit-btn {
                width: 100%;
                max-width: 300px;
                padding: 10px;
                font-size: 16px;
                background-color: #4caf50;
                color: white;
                border: none;
                border-radius: 5px;
                cursor: pointer;
            }

            .submit-btn:hover {
                background-color: #45a049;
            }

            .ips {
                margin-top: 20px;
                overflow-y: auto;
                padding-right: 10px;
            }

            .ip {
                background-color: #e0e0e0;
                margin: 5px 0;
                padding: 10px;
                border-radius: 5px;
                color: #333;
                font-size: 16px;
                word-wrap: break-word;
            }

            .no-ips {
                font-size: 18px;
                color: #888;
                text-align: center;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <form class="form-container" method="POST" action="/send">
                <h1>IP Input</h1>
                <input type="text" name="ip" class="input-field" placeholder="Enter IP" id="ip-input" />
                <input type="file" name="file" class="select-file" id="file-input" />
                <button type="submit" class="submit-btn" id="submit-btn">Submit</button>
                <!-- Added submit button -->
            </form>
            <div class="ips-container">
                <% if (devices && devices.length > 0) { %>
                <h1>List of IPs: <%= devices.length %></h1>
                <% } else { %>
                <p class="no-ips">No IPs available</p>
                <% } %>
                <div class="ips">
                    <% if (devices && devices.length > 0) { %> <% devices.forEach(function(ip) { %>
                    <div class="ip"><%= ip.ip %></div>
                    <% }); %> <% } else { %>
                    <p class="no-ips">No IPs available</p>
                    <% } %>
                </div>
            </div>
        </div>
    </body>
</html>
